@model WebApplication.Models.AccountABSurveysViewModel
@{
    ViewBag.Title = "AlphabetSurveys";
}

<h2>AlphabetSurveys</h2>

<div id="AlphabetSurveyList">
    <table>
        <tr>
            <th>Language</th>
            <th>Date</th>
            <th></th>
        </tr>
        @foreach (var survey in Model.Surveys)
        {
            <tr>
                <td>Icelandic</td>
                <td>@survey.DateFinished</td>
                <td><a href="@Url.Action("Result","Alphabet", new { id = survey.Id })" class="btn btn-success btn-sm">Open</a></td>
            </tr>
        }
    </table>
</div>